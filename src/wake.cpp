#include <opal/wake.hpp>
#include <opal/config.hpp>
#include <opal/crypto.hpp>
#include <opal/tailnet.hpp>

#include <arpa/inet.h>
#include <cerrno>
#include <chrono>
#include <fcntl.h>
#include <poll.h>
#include <sys/socket.h>
#include <unistd.h>

#if defined(__linux__)
#include <linux/ethtool.h>
#include <linux/sockios.h>
#include <net/if.h>
#include <sys/ioctl.h>
#endif

#include <array>
#include <cctype>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <filesystem>
#include <fstream>
#include <iostream>
#include <sstream>
#include <string>
#include <thread>
#include <utility>
#include <vector>

namespace opal { namespace {
void set_cloexec(int fd){if(fd<0)return;const int flags=fcntl(fd,F_GETFD,0);if(flags>=0)(void)fcntl(fd,F_SETFD,flags|FD_CLOEXEC);}
void set_socket_no_sigpipe(int fd){
#ifdef SO_NOSIGPIPE
    int one=1;(void)setsockopt(fd,SOL_SOCKET,SO_NOSIGPIPE,&one,sizeof(one));
#else
    (void)fd;
#endif
}
int send_flags_no_sigpipe(){
#ifdef MSG_NOSIGNAL
    return MSG_NOSIGNAL;
#else
    return 0;
#endif
}
int tcp_socket(){
#ifdef SOCK_CLOEXEC
    int cloexec_fd=socket(AF_INET,SOCK_STREAM|SOCK_CLOEXEC,0);if(cloexec_fd>=0){set_socket_no_sigpipe(cloexec_fd);return cloexec_fd;}
#endif
    int fd=socket(AF_INET,SOCK_STREAM,0);if(fd>=0){set_cloexec(fd);set_socket_no_sigpipe(fd);}return fd;
}
int udp_socket(){int fd=socket(AF_INET,SOCK_DGRAM,0);if(fd>=0){set_cloexec(fd);set_socket_no_sigpipe(fd);}return fd;}
int accept_cloexec(int fd){
#ifdef __linux__
    int cloexec_fd=accept4(fd,nullptr,nullptr,SOCK_CLOEXEC);if(cloexec_fd>=0){set_socket_no_sigpipe(cloexec_fd);return cloexec_fd;}if(errno!=ENOSYS&&errno!=EINVAL)return -1;
#endif
    int c=accept(fd,nullptr,nullptr);if(c>=0){set_cloexec(c);set_socket_no_sigpipe(c);}return c;
}
void socket_deadlines(int fd,int seconds=3){timeval tv{seconds,0};setsockopt(fd,SOL_SOCKET,SO_RCVTIMEO,&tv,sizeof(tv));setsockopt(fd,SOL_SOCKET,SO_SNDTIMEO,&tv,sizeof(tv));}
bool connect_deadline(int fd,const sockaddr_in&a,int timeout_ms){int flags=fcntl(fd,F_GETFL,0);if(flags<0)return false;if(fcntl(fd,F_SETFL,flags|O_NONBLOCK)<0)return false;int rc=connect(fd,reinterpret_cast<const sockaddr*>(&a),sizeof(a));if(rc==0){fcntl(fd,F_SETFL,flags);return true;}if(errno!=EINPROGRESS){fcntl(fd,F_SETFL,flags);return false;}pollfd p{fd,POLLOUT,0};do{rc=poll(&p,1,timeout_ms);}while(rc<0&&errno==EINTR);int err=0;socklen_t n=sizeof(err);bool ok=rc>0&&!(p.revents&(POLLERR|POLLHUP|POLLNVAL))&&getsockopt(fd,SOL_SOCKET,SO_ERROR,&err,&n)==0&&err==0;fcntl(fd,F_SETFL,flags);return ok;}
bool send_all(int fd,const std::string&s){size_t pos=0;while(pos<s.size()){ssize_t n=send(fd,s.data()+pos,s.size()-pos,send_flags_no_sigpipe());if(n>0){pos+=static_cast<size_t>(n);continue;}if(n<0&&errno==EINTR)continue;return false;}return true;}
void bridge_peer(int c,const std::string&secret,const std::string&mac){socket_deadlines(c);auto nonce=random_hex(24);if(!send_all(c,nonce+"\n")){close(c);return;}char buf[512]{};ssize_t n;do{n=recv(c,buf,sizeof(buf)-1,0);}while(n<0&&errno==EINTR);std::string got(buf,n>0?static_cast<size_t>(n):0);got=trim(got);if(n>0&&secure_equal(got,hmac_sha256_hex(secret,nonce))){bool ok=send_wol(mac);send_all(c,ok?"OK\n":"ERR\n");}else send_all(c,"DENY\n");close(c);}
std::string capture_command(const std::string&command){FILE*pipe=popen((command+" 2>/dev/null").c_str(),"r");if(!pipe)return{};std::string output;std::array<char,512>buffer{};while(fgets(buffer.data(),static_cast<int>(buffer.size()),pipe))output+=buffer.data();if(pclose(pipe)!=0)return{};return trim(output);}
}

std::vector<uint8_t>wol_packet(const std::string&mac){std::array<unsigned,6>b{};char c;std::istringstream ss(mac);for(int i=0;i<6;i++){if(!(ss>>std::hex>>b[i])||b[i]>0xff)return{};if(i<5&&(!(ss>>c)||c!=':'))return{};}std::vector<uint8_t>p(102,0xff);for(int r=0;r<16;r++)for(int i=0;i<6;i++)p[6+r*6+i]=static_cast<uint8_t>(b[i]);return p;}
bool send_wol(const std::string&mac,const std::string&broadcast,uint16_t port){auto p=wol_packet(mac);if(p.empty())return false;int fd=udp_socket();if(fd<0)return false;int yes=1;setsockopt(fd,SOL_SOCKET,SO_BROADCAST,&yes,sizeof(yes));sockaddr_in a{};a.sin_family=AF_INET;a.sin_port=htons(port);if(inet_pton(AF_INET,broadcast.c_str(),&a.sin_addr)!=1){close(fd);return false;}auto n=sendto(fd,p.data(),p.size(),send_flags_no_sigpipe(),reinterpret_cast<sockaddr*>(&a),sizeof(a));close(fd);return n==static_cast<ssize_t>(p.size());}
int run_bridge(uint16_t port){Paths p=Paths::load();ensure_layout(p);Ini cfg;if(!cfg.load(p.root/"bridge.ini")){std::cerr<<"bridge not configured; run: opal bridge setup --mac XX:XX:XX:XX:XX:XX\n";return 2;}auto secret=cfg.get("bridge","secret"),mac=cfg.get("bridge","mac");if(secret.empty()||secret.size()<32||wol_packet(mac).empty()){std::cerr<<"bridge configuration invalid\n";return 2;}const auto bind_ip=local_tailnet_ipv4();if(!is_tailnet_ipv4(bind_ip)){std::cerr<<"wake bridge requires a connected Tailscale IPv4 address; refusing to listen publicly\n";return 2;}int fd=tcp_socket();if(fd<0)return 1;int yes=1;setsockopt(fd,SOL_SOCKET,SO_REUSEADDR,&yes,sizeof(yes));sockaddr_in a{};a.sin_family=AF_INET;a.sin_port=htons(port);if(inet_pton(AF_INET,bind_ip.c_str(),&a.sin_addr)!=1||bind(fd,reinterpret_cast<sockaddr*>(&a),sizeof(a))||listen(fd,16)){perror("bridge");close(fd);return 1;}std::cout<<"OPAL wake bridge listening on Tailscale "<<bind_ip<<":"<<port<<"\n";auto worker=[&]{for(;;){int c=accept_cloexec(fd);if(c<0){if(errno==EINTR)continue;std::this_thread::sleep_for(std::chrono::milliseconds(10));continue;}bridge_peer(c,secret,mac);}};std::vector<std::thread>workers;for(int i=0;i<4;++i)workers.emplace_back(worker);for(auto&t:workers)t.join();return 0;}
static bool remote_wake(const std::string&address,uint16_t port,const std::string&secret){if(secret.empty())return false;auto colon=address.rfind(':');std::string host=colon==std::string::npos?address:address.substr(0,colon);if(colon!=std::string::npos){try{int parsed=std::stoi(address.substr(colon+1));if(parsed<1||parsed>65535)return false;port=static_cast<uint16_t>(parsed);}catch(...){return false;}}if(!is_tailnet_ipv4(host))return false;int fd=tcp_socket();if(fd<0)return false;socket_deadlines(fd);sockaddr_in a{};a.sin_family=AF_INET;a.sin_port=htons(port);if(inet_pton(AF_INET,host.c_str(),&a.sin_addr)!=1||!connect_deadline(fd,a,3000)){close(fd);return false;}char b[256]{};ssize_t n;do{n=recv(fd,b,sizeof(b)-1,0);}while(n<0&&errno==EINTR);if(n<=0){close(fd);return false;}auto nonce=trim(std::string(b,static_cast<size_t>(n)));auto proof=hmac_sha256_hex(secret,nonce)+"\n";if(!send_all(fd,proof)){close(fd);return false;}do{n=recv(fd,b,sizeof(b)-1,0);}while(n<0&&errno==EINTR);close(fd);return n>0&&std::string(b,static_cast<size_t>(n)).rfind("OK",0)==0;}
int wake_named(const std::string&name){Paths p=Paths::load();Ini h;if(!h.load(p.hosts)){std::cerr<<"no saved hosts\n";return 2;}auto mac=h.get(name,"mac"),bridge=h.get(name,"wake_bridge"),secret=h.get(name,"wake_secret");if(mac.empty()){std::cerr<<"host has no MAC configured\n";return 2;}bool ok=bridge.empty()?send_wol(mac):remote_wake(bridge,47992,secret);if(ok&&bridge.empty())std::cout<<"local-LAN wake request sent (remote wake needs an OPAL/Tailscale relay on the host LAN)\n";else std::cout<<(ok?"wake request sent\n":"wake failed\n");return ok?0:1;}

#if defined(__linux__)
namespace {
struct WolInfo{bool known=false,supported=false,enabled=false;};
std::string read_text(const std::filesystem::path&path){std::ifstream input(path);std::string value;std::getline(input,value);return trim(value);}
bool physical_interface(const std::filesystem::path&entry){std::error_code error;return std::filesystem::exists(entry/"device",error)&&!error;}
bool wifi_interface(const std::filesystem::path&entry){std::error_code error;return std::filesystem::exists(entry/"wireless",error)&&!error;}
WolInfo ethernet_wol(const std::string&name){WolInfo result;const int fd=socket(AF_INET,SOCK_DGRAM|SOCK_CLOEXEC,0);if(fd<0)return result;ifreq request{};std::strncpy(request.ifr_name,name.c_str(),IFNAMSIZ-1);ethtool_wolinfo info{};info.cmd=ETHTOOL_GWOL;request.ifr_data=reinterpret_cast<char*>(&info);if(ioctl(fd,SIOCETHTOOL,&request)==0){result.known=true;result.supported=(info.supported&WAKE_MAGIC)!=0;result.enabled=(info.wolopts&WAKE_MAGIC)!=0;}close(fd);return result;}
bool set_ethernet_wol(const std::string&name){const int fd=socket(AF_INET,SOCK_DGRAM|SOCK_CLOEXEC,0);if(fd<0)return false;ifreq request{};std::strncpy(request.ifr_name,name.c_str(),IFNAMSIZ-1);ethtool_wolinfo info{};info.cmd=ETHTOOL_GWOL;request.ifr_data=reinterpret_cast<char*>(&info);if(ioctl(fd,SIOCETHTOOL,&request)!=0||!(info.supported&WAKE_MAGIC)){close(fd);return false;}info.cmd=ETHTOOL_SWOL;info.wolopts|=WAKE_MAGIC;const bool ok=ioctl(fd,SIOCETHTOOL,&request)==0;close(fd);return ok;}
std::string active_connection(const std::string&name){if(!command_exists("nmcli"))return{};auto value=capture_command("nmcli -g GENERAL.CONNECTION device show "+shell_quote(name));return value=="--"?std::string{}:value;}
bool nm_persistent(const std::string&name,bool wifi){auto connection=active_connection(name);if(connection.empty())return false;const auto key=wifi?"802-11-wireless.wake-on-wlan":"802-3-ethernet.wake-on-lan";return capture_command("nmcli -g "+std::string(key)+" connection show "+shell_quote(connection)).find("magic")!=std::string::npos;}
bool set_nm_persistent(const std::string&name,bool wifi){auto connection=active_connection(name);if(connection.empty())return false;const auto key=wifi?"802-11-wireless.wake-on-wlan":"802-3-ethernet.wake-on-lan";if(std::system(("nmcli connection modify "+shell_quote(connection)+" "+key+" magic").c_str())!=0)return false;(void)std::system(("nmcli device reapply "+shell_quote(name)+" >/dev/null 2>&1").c_str());return true;}
bool install_iw(){if(command_exists("iw"))return true;std::string prefix;if(geteuid()!=0){if(!command_exists("sudo"))return false;prefix="sudo ";}std::string install;if(command_exists("dnf"))install="dnf -y install iw";else if(command_exists("apt-get"))install="apt-get install -y iw";else if(command_exists("pacman"))install="pacman -S --needed --noconfirm iw";else if(command_exists("zypper"))install="zypper --non-interactive install iw";else if(command_exists("apk"))install="apk add iw";else return false;std::cout<<"Installing Linux WoWLAN helper (iw)...\n";if(std::system((prefix+install).c_str())!=0)return false;return command_exists("iw");}
std::string wifi_phy(const std::filesystem::path&entry){std::error_code error;auto target=std::filesystem::read_symlink(entry/"phy80211",error);return error?std::string{}:target.filename().string();}
WolInfo wifi_wol(const std::filesystem::path&entry){WolInfo result;if(!command_exists("iw"))return result;auto phy=wifi_phy(entry);if(phy.empty())return result;auto info=capture_command("iw phy "+shell_quote(phy)+" info");if(info.empty())return result;result.known=true;const auto section=info.find("WoWLAN support:");result.supported=section!=std::string::npos&&info.find("magic packet",section)!=std::string::npos;auto current=capture_command("iw phy "+shell_quote(phy)+" wowlan show");result.enabled=current.find("magic packet")!=std::string::npos||current.find("magic-packet")!=std::string::npos;return result;}
bool set_wifi_magic(const std::filesystem::path&entry){if(!command_exists("iw"))return false;auto phy=wifi_phy(entry);if(phy.empty())return false;return std::system(("iw phy "+shell_quote(phy)+" wowlan enable magic-packet >/dev/null 2>&1").c_str())==0;}
std::string self_executable(){std::array<char,4096>buffer{};auto length=readlink("/proc/self/exe",buffer.data(),buffer.size()-1);return length>0?std::string(buffer.data(),static_cast<std::size_t>(length)):std::string{};}
std::filesystem::path systemd_link_path(const std::string&mac){std::string id;for(char c:mac)if(c!=':'&&c!='-')id+=c;return std::filesystem::path("/etc/systemd/network")/("90-opal-wol-"+id+".link");}
bool systemd_persistent(const std::string&mac){if(mac.empty())return false;std::error_code error;return std::filesystem::exists(systemd_link_path(mac),error)&&!error;}
bool write_systemd_persistent(const std::string&mac){if(mac.empty())return false;std::error_code error;if(!std::filesystem::exists("/run/systemd/system",error)||error)return false;std::filesystem::create_directories("/etc/systemd/network",error);if(error)return false;std::ofstream output(systemd_link_path(mac),std::ios::out|std::ios::trunc);if(!output)return false;output<<"[Match]\nMACAddress="<<mac<<"\n\n[Link]\nWakeOnLan=magic\n";return output.good();}
bool request_admin_config(const std::string&name,const std::string&mac){auto executable=self_executable();if(executable.empty()||!command_exists("sudo"))return false;return std::system(("sudo "+shell_quote(executable)+" --internal-wake-config "+shell_quote(name)+" "+shell_quote(mac)).c_str())==0;}
}
int configure_linux_wake_admin(const std::string&name,const std::string&mac){if(geteuid()!=0)return 5;auto entry=std::filesystem::path("/sys/class/net")/name;if(!physical_interface(entry)||read_text(entry/"address")!=mac)return 4;const bool wifi=wifi_interface(entry);if(wifi&&!install_iw())return 3;const bool configured=wifi?set_wifi_magic(entry):set_ethernet_wol(name);bool persistent=false;if(command_exists("nmcli"))persistent=set_nm_persistent(name,wifi);if(!wifi&&!persistent)persistent=write_systemd_persistent(mac);(void)persistent;return configured?0:3;}
WakeCapabilityReport configure_host_wake(){WakeCapabilityReport report;std::error_code error;for(const auto&entry:std::filesystem::directory_iterator("/sys/class/net",error)){if(error)break;auto name=entry.path().filename().string();if(name=="lo"||!physical_interface(entry.path()))continue;WakeAdapterCapability adapter;adapter.name=name;adapter.description=name;adapter.mac=read_text(entry.path()/"address");auto state=read_text(entry.path()/"operstate");adapter.connected=state=="up"||state=="unknown";const bool wifi=wifi_interface(entry.path());adapter.link=wifi?WakeLink::Wifi:WakeLink::Ethernet;if(wifi){const bool have_iw=install_iw();auto wol=have_iw?wifi_wol(entry.path()):WolInfo{};adapter.magic_packet_known=wol.known;adapter.magic_packet=wol.supported;adapter.configured=wol.enabled;adapter.persistent=nm_persistent(name,true);if(wol.supported&&!wol.enabled){bool changed=geteuid()==0?configure_linux_wake_admin(name,adapter.mac)==0:request_admin_config(name,adapter.mac);if(changed){wol=wifi_wol(entry.path());adapter.magic_packet_known=wol.known;adapter.magic_packet=wol.supported;adapter.configured=wol.enabled;adapter.persistent=nm_persistent(name,true);}}if(!have_iw){adapter.sleep=adapter.hibernate=adapter.shutdown=WakePowerSupport::Unknown;adapter.limitation="OPAL could not install the Linux wireless tool (iw), so WoWLAN cannot be verified. Install iw or use Ethernet.";}else if(adapter.magic_packet){adapter.sleep=adapter.configured?WakePowerSupport::Yes:WakePowerSupport::Unknown;adapter.hibernate=WakePowerSupport::Unknown;adapter.shutdown=WakePowerSupport::No;adapter.limitation=adapter.configured?"Wi-Fi uses WoWLAN. Sleep wake is available; hibernate depends on the chipset/firmware and shutdown wake is not assumed.":"This Wi-Fi adapter supports Magic Packet WoWLAN but OPAL could not enable it. Administrator permission or the active network stack may be blocking the change.";}else if(adapter.magic_packet_known){adapter.sleep=adapter.hibernate=adapter.shutdown=WakePowerSupport::No;adapter.limitation="This Wi-Fi adapter/driver does not advertise Magic Packet WoWLAN. Connect Ethernet for reliable remote wake.";}else{adapter.sleep=adapter.hibernate=adapter.shutdown=WakePowerSupport::Unknown;adapter.limitation="WoWLAN capability could not be verified. Use an updated Wi-Fi driver/firmware or connect Ethernet.";}}else{auto wol=ethernet_wol(name);adapter.magic_packet_known=wol.known;adapter.magic_packet=wol.supported;adapter.configured=wol.enabled;adapter.persistent=nm_persistent(name,false)||systemd_persistent(adapter.mac);if(wol.supported&&(!wol.enabled||!adapter.persistent)){bool changed=geteuid()==0?configure_linux_wake_admin(name,adapter.mac)==0:request_admin_config(name,adapter.mac);if(changed){wol=ethernet_wol(name);adapter.magic_packet_known=wol.known;adapter.magic_packet=wol.supported;adapter.configured=wol.enabled;adapter.persistent=nm_persistent(name,false)||systemd_persistent(adapter.mac);}}if(adapter.magic_packet){adapter.sleep=adapter.configured?WakePowerSupport::Yes:WakePowerSupport::Unknown;adapter.hibernate=WakePowerSupport::Unknown;adapter.shutdown=WakePowerSupport::Unknown;adapter.limitation=adapter.configured?"Hibernate/shutdown wake also requires NIC standby power and firmware PCIe/PME wake; disable ErP if the NIC loses power when off.":"This Ethernet adapter supports Magic Packet wake but OPAL could not enable it. Administrator permission or the network driver may be blocking the change.";}else if(adapter.magic_packet_known){adapter.sleep=adapter.hibernate=adapter.shutdown=WakePowerSupport::No;adapter.limitation="The Ethernet driver reports that Magic Packet wake is unsupported.";}else{adapter.sleep=adapter.hibernate=adapter.shutdown=WakePowerSupport::Unknown;adapter.limitation="The Ethernet driver's wake capabilities could not be queried.";}}if(adapter.configured&&!adapter.persistent)adapter.limitation+=" Wake is enabled now but this Linux networking stack could not be made persistent automatically.";report.adapters.push_back(std::move(adapter));}report.preferred=preferred_wake_adapter(report.adapters);report.notes.push_back("OPAL never exposes UDP 7/9 to the Internet. Remote wake outside the host LAN requires an always-on OPAL/Tailscale relay on that LAN.");report.notes.push_back("A powered-off NIC cannot run Tailscale itself. Tailscale transports the authenticated request to the relay; the relay emits the local wake packet.");report.notes.push_back("No software wake works after AC power is removed, the PSU is switched off, or firmware removes NIC standby power.");return report;}
#elif defined(__APPLE__)
namespace {
bool contains_case_insensitive(std::string text,std::string needle){for(char&c:text)c=static_cast<char>(std::tolower(static_cast<unsigned char>(c)));for(char&c:needle)c=static_cast<char>(std::tolower(static_cast<unsigned char>(c)));return text.find(needle)!=std::string::npos;}
bool pmset_womp_enabled(){auto output=capture_command("/usr/bin/pmset -g custom");return output.find("womp")!=std::string::npos&&output.find("womp 1")!=std::string::npos;}
bool enable_pmset_womp(){if(pmset_womp_enabled())return true;if(geteuid()==0)return std::system("/usr/bin/pmset -a womp 1")==0;if(!command_exists("sudo"))return false;return std::system("sudo /usr/bin/pmset -a womp 1") == 0;}
bool interface_active(const std::string&name){return capture_command("/sbin/ifconfig "+shell_quote(name)).find("status: active")!=std::string::npos;}
WakeLink classify_port(const std::string&port){if(contains_case_insensitive(port,"wi-fi")||contains_case_insensitive(port,"wifi")||contains_case_insensitive(port,"airport"))return WakeLink::Wifi;if(contains_case_insensitive(port,"ethernet")||contains_case_insensitive(port,"lan"))return WakeLink::Ethernet;return WakeLink::Other;}
void add_macos_adapter(WakeCapabilityReport&report,const std::string&port,const std::string&device,const std::string&mac,bool womp){if(device.empty()||mac.empty())return;WakeAdapterCapability adapter;adapter.name=device;adapter.description=port.empty()?device:port;adapter.mac=mac;adapter.link=classify_port(port);adapter.connected=interface_active(device);if(adapter.link==WakeLink::Ethernet){adapter.magic_packet_known=true;adapter.magic_packet=womp;adapter.configured=womp;adapter.persistent=womp;adapter.sleep=womp?WakePowerSupport::Yes:WakePowerSupport::Unknown;adapter.hibernate=WakePowerSupport::Unknown;adapter.shutdown=WakePowerSupport::No;adapter.limitation=womp?"macOS network wake is configured for sleep. Traditional PC-style shutdown (S5) Wake-on-LAN is not assumed on Mac hardware; external adapters/docks must retain standby power.":"macOS Wake for network access could not be enabled. Administrator permission or hardware support may be required.";}else if(adapter.link==WakeLink::Wifi){adapter.magic_packet_known=false;adapter.configured=womp;adapter.persistent=womp;adapter.sleep=WakePowerSupport::Unknown;adapter.hibernate=WakePowerSupport::Unknown;adapter.shutdown=WakePowerSupport::No;adapter.limitation="macOS can use Wake for network access while sleeping, but direct Wi-Fi Magic Packet wake depends on the Mac, access point and sleep mode. Shutdown wake is not supported by OPAL over Wi-Fi.";}else{adapter.magic_packet_known=false;adapter.configured=womp;adapter.persistent=womp;adapter.sleep=WakePowerSupport::Unknown;adapter.hibernate=WakePowerSupport::Unknown;adapter.shutdown=WakePowerSupport::No;adapter.limitation="This network hardware is not a standard Ethernet/Wi-Fi wake target. OPAL cannot guarantee wake support.";}report.adapters.push_back(std::move(adapter));}
}
WakeCapabilityReport configure_host_wake(){WakeCapabilityReport report;const bool womp=enable_pmset_womp()&&pmset_womp_enabled();auto output=capture_command("/usr/sbin/networksetup -listallhardwareports");std::string port,device,mac;std::size_t start=0;while(start<=output.size()){auto end=output.find('\n',start);auto line=trim(output.substr(start,end==std::string::npos?std::string::npos:end-start));if(line.rfind("Hardware Port:",0)==0){add_macos_adapter(report,port,device,mac,womp);port=trim(line.substr(std::string("Hardware Port:").size()));device.clear();mac.clear();}else if(line.rfind("Device:",0)==0)device=trim(line.substr(std::string("Device:").size()));else if(line.rfind("Ethernet Address:",0)==0)mac=trim(line.substr(std::string("Ethernet Address:").size()));if(end==std::string::npos)break;start=end+1;}add_macos_adapter(report,port,device,mac,womp);report.preferred=preferred_wake_adapter(report.adapters);if(!womp)report.notes.push_back("OPAL could not enable macOS 'Wake for network access'. If macOS requested administrator authorization, approve it and run setup again.");report.notes.push_back("On Mac hosts, OPAL treats network wake as a sleep feature; it does not promise wake from a fully shut-down Mac.");report.notes.push_back("Remote wake from another network still requires an always-on OPAL/Tailscale relay on the Mac's LAN.");report.notes.push_back("No software wake works when the Mac has no power or an external Ethernet adapter/dock loses standby power.");return report;}
#else
WakeCapabilityReport configure_host_wake(){WakeCapabilityReport report;report.notes.push_back("Wake capability detection is not implemented on this operating system.");return report;}
#endif
}
